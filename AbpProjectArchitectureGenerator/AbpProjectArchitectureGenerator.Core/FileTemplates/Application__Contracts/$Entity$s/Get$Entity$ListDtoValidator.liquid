{{- if model.includelocalusing -}}
using FluentValidation;
{{ raw }}
{{ endraw }}
{{- end -}}
namespace {{ model.projectnamespace }}.{{ model.entityname }}s
{
    public class Get{{ model.entityname }}ListDtoValidator : AbstractValidator<Get{{ model.entityname }}ListDto> 
    {
        public Get{{ model.entityname }}ListDtoValidator()
        {

        }
    }
}