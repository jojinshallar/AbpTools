{{- if model.includelocalusing -}}
using FluentValidation;
{{ raw }}
{{ endraw }}
{{- end -}}
namespace {{ model.projectnamespace }}.{{ model.entityname }}s
{
    public class Create{{ model.entityname }}DtoValidator : AbstractValidator<Create{{ model.entityname }}Dto> 
    {
        public Create{{ model.entityname }}DtoValidator()
        {

        }
    }
}